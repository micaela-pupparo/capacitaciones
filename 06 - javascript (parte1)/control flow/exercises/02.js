//                      EXERCISE -- LANDSCAPE OF PORTRAIT
// LANDSCAPE = WIDTH > HEIGHT

function isLandscape(width, height) {
    return (width > height); // ? true : false
    //esto hace lo mismo y limpia el codigo
}

console.log(isLandscape(25,1));